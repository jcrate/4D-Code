  // ----------------------------------------------------  // User name (OS): James Crate  // Date and time: 07/18/11, 13:42:07  // ----------------------------------------------------  // Method: CODEC_Encode_Base64_x  //   // base64-encodes text  //  // ----------------------------------------------------C_TEXT($0)C_TEXT($1;$textToEncode)$textToEncode:=$1  // ----------------------------------------------------C_BLOB($encoded)  // ----------------------------------------------------TEXT TO BLOB($textToEncode;$encoded;Mac text without length;*)BASE64 ENCODE($encoded)$0:=BLOB to text($encoded;Mac text without length)